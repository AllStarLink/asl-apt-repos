# ASL Apt Repositories
This package installs the AllStarLink package repositories.
To install the repository simply run the following commands
on a Debian 12 Bookworm or Debian 11 Buster system:

```
wget -O/tmp/a
dpkg -i/tmp/
```
